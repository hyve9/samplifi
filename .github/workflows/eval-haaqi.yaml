name: Evaluate Samplifi on mirdata datasets using HAAQi

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Conda
      uses: conda-incubator/setup-miniconda@v2
      with:
        activate-environment: samplifi
        environment-file: environment.yml

    - name: Install dependencies
      run: |
        source activate samplifi
        pip install -r requirements.txt

    - name: Run Samplifi
      run: python samplifi --score --mirdata

    - name: Archive artifacts
      uses: actions/upload-artifact@v2
      with:
        name: outputs
        path: output_folder/
